import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  textAreaContainer: {
    borderColor: '#979797',
    borderWidth: 1,
    borderRadius: 4,
    marginTop: 8,
    padding: 4,
    marginBottom: 9,
  },
  textarea: {
    padding: 8,
    borderRadius: 4,
  },
});
