import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    backgroundColor: '#3858C1',
    borderRadius: 8,
    height: 64,
    marginBottom: 16,
    justifyContent: 'center',
    alignItems: 'center',
    width: '100%',
  },
  buttonLabel: {
    color: '#fff',
    fontSize: 22,
  },
});
